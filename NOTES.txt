Optimizing for more parameters -- Penalize large parameter counts.

Make it so that mutations dont reliably increase parameter count, and just make super big networks


One output layer should be fine

do it in repeating "blocks", how most convs are structured

Other benchmarks like autoencoder